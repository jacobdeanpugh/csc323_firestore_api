{
  "name": "vote-counter",
  "version": "1.0.0",
  "description": "Firestore Trigger Cloud Function for Vote Processing",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions:voteCounter"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "engines": {
    "node": "18"
  }
}
